{
  "$comment": "Claude Code hooks for protect-mcp. Merge into .claude/settings.json. Claude Code hands each hook the call as JSON on stdin; --format claude reads tool_name and tool_input from it. PreToolUse evaluates the call against ./cedar and blocks a deny (exit 2, with the reason returned to the model); PostToolUse signs a receipt for every call. A missing policy denies, fail-closed.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "npx protect-mcp@0.14.0 evaluate --cedar ./cedar --format claude",
            "timeout": 30
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "npx protect-mcp@0.14.0 sign --format claude --receipts ./receipts --key ./keys/gateway.json",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
