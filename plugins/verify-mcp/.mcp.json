{
  "mcpServers": {
    "verify-mcp": {
      "command": "npx",
      "args": ["-y", "@scopeblind/verify-mcp"],
      "description": "Offline verification of signed receipts, bundles, and trust artifacts. No network calls."
    }
  }
}
