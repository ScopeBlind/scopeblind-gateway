{
  "mcpServers": {
    "protect-mcp": {
      "command": "npx",
      "args": [
        "-y",
        "protect-mcp",
        "serve",
        "--enforce"
      ],
      "description": "Ed25519 receipt signing + Cedar policy enforcement for tool calls"
    }
  }
}
